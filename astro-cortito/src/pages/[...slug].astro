---
import getOneShortcut from "../libs/getOneShortcut.js";

const { slug } = Astro.params;

const page = await getOneShortcut(slug);

if (page) return Astro.redirect(`${page.oldLink}`);
if (!page) return Astro.redirect("/404");
---
